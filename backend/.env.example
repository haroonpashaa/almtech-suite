PORT=5050
NODE_ENV=development
# Set to "embedded" (or leave blank) to use bundled MongoDB.
# Or paste a real MongoDB connection string when you have one.
MONGO_URI=embedded
JWT_SECRET=replace_me_with_a_long_random_string
JWT_EXPIRES_IN=12h
CORS_ORIGIN=http://localhost:5174
